Description: IT - templating {{ stack.tags.hello }}
Resources:

  {{#each stack.data.queueNames}}
  Queue{{@index}}:
    Type: AWS::Logs::LogGroup
    Properties:
      LogGroupName: {{this}}
  {{/each}}
